name: Staging CICD Development

on:
  push:
    branches:
      - "staging/*"

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run Test
        run: echo "Running on the staging branch"

      - name: Deploy
        run: echo "Deploying onto the staging branch"
